# Nixpacks configuration for Railway
# Ensures Node.js 20 is used (npm is bundled with nodejs)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Skip automatic npm install to prevent issues with workspace dependencies
# Installation is handled in the buildCommand
cmds = ["echo 'Installation handled in buildCommand'"]

