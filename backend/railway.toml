# Railway configuration for Backend Service
# NestJS API server

[build]
builder = "nixpacks"
# Build command is now handled in nixpacks.toml to avoid automatic npm install issues

[deploy]
startCommand = "node dist/main.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30

